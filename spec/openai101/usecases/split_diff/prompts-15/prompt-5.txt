In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: app/controllers/tasks_controller.rb
Apply the fix to this code:
@@ -24,7 +24,7 @@ class TasksController < ApplicationController
-      notification_ids: notification_ids
+      notification_ids:

Apply the fix to this code:
@@ -110,7 +110,7 @@ class TasksController < ApplicationController
-    @context.update(end_date: end_date)
+    @context.update(end_date:)

Apply the fix to this code:
@@ -183,7 +183,7 @@ class TasksController < ApplicationController
 
-        @task.assign_attributes(taskable_type: taskable_type, taskable_id: taskable_id)
+        @task.assign_attributes(taskable_type:, taskable_id:)

Apply the fix to this code:
@@ -202,10 +202,10 @@ class TasksController < ApplicationController
-      completed_at: completed_at,
+      completed_at:,
-      notification_ids: notification_ids
+      notification_ids:

Apply the fix to this code:
@@ -310,7 +310,7 @@ class TasksController < ApplicationController
-      completed_at: completed_at
+      completed_at:

File: app/controllers/tokens_controller.rb
Apply the fix to this code:
@@ -17,7 +17,7 @@ class TokensController < ApplicationController
-      render json: { user_ids: user_ids, user_tokens: user_tokens }
+      render json: { user_ids:, user_tokens: }

File: app/controllers/tracker_controller.rb
Apply the fix to this code:
@@ -88,7 +88,7 @@ class TrackerController < ApplicationController
-          template_merger = Campaign.merger(campaign: campaign, contact: contact, tenant: tenant, identity: identity)
+          template_merger = Campaign.merger(campaign:, contact:, tenant:, identity:)

File: app/controllers/users_controller.rb
Apply the fix to this code:
@@ -108,7 +108,7 @@ class UsersController < ApplicationController
 
-    @context.update(assigned_user_id: assigned_user_id)
+    @context.update(assigned_user_id:)

File: app/helpers/ai_helper.rb
Apply the fix to this code:
@@ -14,7 +14,7 @@ module AiHelper
-      prompt_link(prompt, model: template.model, identifier: identifier, template_id: template.id, attributes: attributes, &block)
+      prompt_link(prompt, model: template.model, identifier:, template_id: template.id, attributes:, &block)

File: app/helpers/application_helper.rb
Apply the fix to this code:
@@ -73,7 +73,7 @@ module ApplicationHelper
-    link_to(name, "#", class: "add_fields", data: {id: id, fields: fields.gsub("\n", "")})
+    link_to(name, "#", class: "add_fields", data: {id:, fields: fields.gsub("\n", "")})

Apply the fix to this code:
@@ -85,7 +85,7 @@ module ApplicationHelper
-    link_to title, params.merge(sort: column, direction: direction, page: nil), {class: css_class}
+    link_to title, params.merge(sort: column, direction:, page: nil), {class: css_class}

Apply the fix to this code:
@@ -266,7 +266,7 @@ module ApplicationHelper
 
-    link_to contact_path(contact), target: target do
+    link_to(contact_path(contact), target:) do

File: app/helpers/cogs_helper.rb
Apply the fix to this code:
@@ -33,7 +33,7 @@ module CogsHelper
 
-    number_with_precision(cogs_percentage, precision: precision)
+    number_with_precision(cogs_percentage, precision:)

File: app/helpers/growth_helper.rb
Apply the fix to this code:
@@ -35,7 +35,7 @@ module GrowthHelper
-    icon = growth >= 0.0 ? stream_icon({ name: "performance-increase.svg", color: color, size: "14px", top: "-2px" }) : stream_icon({ name: "performance-decrease.svg", color: color, size: "14px", top: "-2px" })
+    icon = growth >= 0.0 ? stream_icon({ name: "performance-increase.svg", color:, size: "14px", top: "-2px" }) : stream_icon({ name: "performance-decrease.svg", color:, size: "14px", top: "-2px" })

Apply the fix to this code:
@@ -59,7 +59,7 @@ module GrowthHelper
-    icon = risk_level >= 0.0 ? material_icon({ name: "check_circle", size: 16, color: color, top: "-1px"}) : material_icon({ name: "error", size: 16, color: color, top: "-1px"})
+    icon = risk_level >= 0.0 ? material_icon({ name: "check_circle", size: 16, color:, top: "-1px"}) : material_icon({ name: "error", size: 16, color:, top: "-1px"})

File: app/interactors/inquiries/create.rb
Apply the fix to this code:
@@ -215,7 +215,7 @@ module Inquiries
 
-        tenant: tenant,
+        tenant:,

File: app/interactors/sales_summaries/pack_and_send/generate_stats.rb
Apply the fix to this code:
@@ -294,11 +294,11 @@ module SalesSummaries
-            tenant: tenant,
+            tenant:,
-            accounting_month: accounting_month,
-            accounting_year: accounting_year
+            accounting_month:,
+            accounting_year:

Apply the fix to this code:
@@ -350,10 +350,10 @@ module SalesSummaries
 
-          tenant: tenant,
+          tenant:,
-          accounting_month: accounting_month,
-          accounting_year: accounting_year
+          accounting_month:,
+          accounting_year:

File: app/models/ai_response.rb
Apply the fix to this code:
@@ -15,13 +15,13 @@ class AiResponse < ApplicationRecord
-      tenant: tenant,
-      prompt: prompt,
-      response: response,
-      model: model,
-      template_id: template_id,
+      tenant:,
+      prompt:,
+      response:,
+      model:,
+      template_id:,
-      messages: messages
+      messages:


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
