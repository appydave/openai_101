In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: spec/requests/api/v1/inquiries_spec.rb
Apply the fix to this code:
@@ -5,8 +5,8 @@ RSpec.describe Api::V1::InquiriesController, type: :request do
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, tenant_picker_name: "Test-1") }
-  let(:tenant_2) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise, tenant_picker_name: "Test-2") }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, tenant_picker_name: "Test-1") }
+  let(:tenant_2) { FactoryBot.create(:tenant, :trait2, enterprise:, tenant_picker_name: "Test-2") }

Apply the fix to this code:
@@ -14,9 +14,9 @@ RSpec.describe Api::V1::InquiriesController, type: :request do
-      token: token,
-      enterprise: enterprise,
-      tenant: tenant,
+      token:,
+      enterprise:,
+      tenant:,

File: spec/requests/api/v1/tenants_spec.rb
Apply the fix to this code:
@@ -5,8 +5,8 @@ RSpec.describe Api::V1::TenantsController, type: :request do
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
-  let(:tenant_2) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }
+  let(:tenant_2) { FactoryBot.create(:tenant, :trait2, enterprise:) }

Apply the fix to this code:
@@ -14,9 +14,9 @@ RSpec.describe Api::V1::TenantsController, type: :request do
-      token: token,
-      enterprise: enterprise,
-      tenant: tenant,
+      token:,
+      enterprise:,
+      tenant:,

Apply the fix to this code:
@@ -82,7 +82,7 @@ RSpec.describe Api::V1::TenantsController, type: :request do
 
-          put "/api/v1/tenants/#{tenant.id}", params: params, headers: { Authorization:  "Token 123"}
+          put "/api/v1/tenants/#{tenant.id}", params:, headers: { Authorization:  "Token 123"}

File: spec/requests/assets_spec.rb
Apply the fix to this code:
@@ -87,7 +87,7 @@ RSpec.describe AssetsController, type: :request do
 
-            post "/asset_manager", params: params
+            post("/asset_manager", params:)

File: spec/requests/bookmarks_spec.rb
Apply the fix to this code:
@@ -18,8 +18,8 @@ RSpec.describe BookmarksController, type: :request do
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:company) { FactoryBot.create(:company, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:company) { FactoryBot.create(:company, :trait1, tenant:) }

Apply the fix to this code:
@@ -44,7 +44,7 @@ RSpec.describe BookmarksController, type: :request do
 
-            post "/bookmarks", params: params
+            post("/bookmarks", params:)

File: spec/requests/budgets_spec.rb
Apply the fix to this code:
@@ -12,14 +12,14 @@ RSpec.describe "BudgetsController", type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:budget) { FactoryBot.create(:budget, :trait1, tenant: tenant, financial_year: DateTime.now.year) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:budget) { FactoryBot.create(:budget, :trait1, tenant:, financial_year: DateTime.now.year) }

Apply the fix to this code:
@@ -84,7 +84,7 @@ RSpec.describe "BudgetsController", type: :request do
 
-            post "/budgets", params: params
+            post("/budgets", params:)

Apply the fix to this code:
@@ -109,7 +109,7 @@ RSpec.describe "BudgetsController", type: :request do
 
-            put "/budgets/#{budget.id}", params: params
+            put("/budgets/#{budget.id}", params:)

Apply the fix to this code:
@@ -121,7 +121,7 @@ RSpec.describe "BudgetsController", type: :request do
 
-        let(:signin_user) { FactoryBot.create(:user, :trait3, *user_traits, **user_values, role: "Enterprise User", token: token_trait3, enterprise: tenant.enterprise, tenant: tenant) }
+        let(:signin_user) { FactoryBot.create(:user, :trait3, *user_traits, **user_values, role: "Enterprise User", token: token_trait3, enterprise: tenant.enterprise, tenant:) }

File: spec/requests/builds_spec.rb
Apply the fix to this code:
@@ -12,13 +12,13 @@ RSpec.describe "BuildsController", type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:build) { FactoryBot.create(:build, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:build) { FactoryBot.create(:build, :trait1, tenant:) }

Apply the fix to this code:
@@ -64,7 +64,7 @@ RSpec.describe "BuildsController", type: :request do
 
-            post "/builds", params: params
+            post("/builds", params:)

File: spec/requests/business_plan_equipments_spec.rb
Apply the fix to this code:
@@ -12,14 +12,14 @@ RSpec.describe "BusinessPlanEquipmentsController", type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant: tenant) }
-  let(:business_plan_equipment) { FactoryBot.create(:business_plan_equipment, :trait1, business_plan: business_plan) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant:) }
+  let(:business_plan_equipment) { FactoryBot.create(:business_plan_equipment, :trait1, business_plan:) }

Apply the fix to this code:
@@ -117,7 +117,7 @@ RSpec.describe "BusinessPlanEquipmentsController", type: :request do
 
-            post "/business_plans/#{business_plan.id}/business_plan_equipments", params: params
+            post("/business_plans/#{business_plan.id}/business_plan_equipments", params:)

File: spec/requests/business_plan_goal_items_spec.rb
Apply the fix to this code:
@@ -12,14 +12,14 @@ RSpec.describe BusinessPlanGoalItemsController, type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant: tenant) }
-  let(:business_plan_goal_item) { FactoryBot.create(:business_plan_goal_item, :trait1, business_plan: business_plan) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant:) }
+  let(:business_plan_goal_item) { FactoryBot.create(:business_plan_goal_item, :trait1, business_plan:) }

File: spec/requests/business_plan_goals_spec.rb
Apply the fix to this code:
@@ -12,14 +12,14 @@ RSpec.describe BusinessPlanGoalsController, type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant: tenant) }
-  let(:business_plan_goal) { FactoryBot.create(:business_plan_goal, :trait1, business_plan: business_plan, business_plan_kpi_id: business_plan_kpi.id) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant:) }
+  let(:business_plan_goal) { FactoryBot.create(:business_plan_goal, :trait1, business_plan:, business_plan_kpi_id: business_plan_kpi.id) }

File: spec/requests/business_plan_marketing_activities_controller_spec.rb
Apply the fix to this code:
@@ -12,14 +12,14 @@ RSpec.describe BusinessPlanMarketingActivitiesController, type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant: tenant) }
-  let(:business_plan_marketing_activity) { FactoryBot.create(:business_plan_marketing_activity, :trait1, business_plan: business_plan) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant:) }
+  let(:business_plan_marketing_activity) { FactoryBot.create(:business_plan_marketing_activity, :trait1, business_plan:) }

Apply the fix to this code:
@@ -73,7 +73,7 @@ RSpec.describe BusinessPlanMarketingActivitiesController, type: :request do
 
-            post "/business_plans/#{business_plan.id}/business_plan_marketing_activities", params: params
+            post("/business_plans/#{business_plan.id}/business_plan_marketing_activities", params:)


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
