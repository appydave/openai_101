In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: spec/requests/phone_calls_spec.rb
Apply the fix to this code:
@@ -11,19 +11,19 @@ RSpec.describe PhoneCallsController, type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:company) { FactoryBot.create(:company, :trait2, tenant: tenant) }
-  let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant: tenant, company: company) }
-  let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant, mobile: "1235") }
-  let!(:phone_call) { FactoryBot.create(:phone_call, :trait1, tenant: tenant, phoneable_id: contact.id, phoneable_type: "Contact", twillio_message_sid: "1234", caller_user_id: signin_user.id, user_id: signin_user.id) }
-  let!(:phone_call_2) { FactoryBot.create(:phone_call, :trait1, tenant: tenant, phoneable_id: estimate.id, phoneable_type: "Estimate", caller_user_id: signin_user.id, user_id: signin_user.id) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:company) { FactoryBot.create(:company, :trait2, tenant:) }
+  let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant:, company:) }
+  let(:contact) { FactoryBot.create(:contact, :trait1, tenant:, mobile: "1235") }
+  let!(:phone_call) { FactoryBot.create(:phone_call, :trait1, tenant:, phoneable_id: contact.id, phoneable_type: "Contact", twillio_message_sid: "1234", caller_user_id: signin_user.id, user_id: signin_user.id) }
+  let!(:phone_call_2) { FactoryBot.create(:phone_call, :trait1, tenant:, phoneable_id: estimate.id, phoneable_type: "Estimate", caller_user_id: signin_user.id, user_id: signin_user.id) }

Apply the fix to this code:
@@ -41,7 +41,7 @@ RSpec.describe PhoneCallsController, type: :request do
-          get "/phone_calls", params: params
+          get("/phone_calls", params:)

Apply the fix to this code:
@@ -59,7 +59,7 @@ RSpec.describe PhoneCallsController, type: :request do
 
-          get new_phone_call_path(), params: params, headers: {  }
+          get new_phone_call_path(), params:, headers: {  }

Apply the fix to this code:
@@ -79,7 +79,7 @@ RSpec.describe PhoneCallsController, type: :request do
 
-          get edit_phone_call_path(phone_call.id), params: params, headers: {  }
+          get edit_phone_call_path(phone_call.id), params:, headers: {  }

Apply the fix to this code:
@@ -125,7 +125,7 @@ RSpec.describe PhoneCallsController, type: :request do
 
-            post "/phone_calls", params: params
+            post("/phone_calls", params:)

File: spec/requests/portal/dashboard_spec.rb
Apply the fix to this code:
@@ -12,15 +12,15 @@ RSpec.describe "Portal::DashboardController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }

File: spec/requests/portal/estimate_spec.rb
Apply the fix to this code:
@@ -11,19 +11,19 @@ RSpec.describe "Portal::EstimateController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }
 
-    let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant: tenant, portal_key: "12345") }
+    let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant:, portal_key: "12345") }

Apply the fix to this code:
@@ -58,7 +58,7 @@ RSpec.describe "Portal::EstimateController", type: :request do
 
-            post "/portal/estimate/12345", params: params
+            post("/portal/estimate/12345", params:)

File: spec/requests/portal/estimates_spec.rb
Apply the fix to this code:
@@ -8,16 +8,16 @@ RSpec.describe "Portal::EstimatesController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant,
+      enterprise:,
+      tenant:,
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
-  let(:contact) { FactoryBot.create(:contact, :trait1,  tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }
+  let(:contact) { FactoryBot.create(:contact, :trait1,  tenant:) }

File: spec/requests/portal/orders_spec.rb
Apply the fix to this code:
@@ -10,8 +10,8 @@ RSpec.describe "Portal::OrdersController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:

Apply the fix to this code:
@@ -19,7 +19,7 @@ RSpec.describe "Portal::OrdersController", type: :request do
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }

File: spec/requests/portal/proofs_spec.rb
Apply the fix to this code:
@@ -10,8 +10,8 @@ RSpec.describe "Portal::ProofsController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:

File: spec/requests/portal/sale_spec.rb
Apply the fix to this code:
@@ -10,15 +10,15 @@ RSpec.describe "Portal::SaleController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }

File: spec/requests/portal/sales_spec.rb
Apply the fix to this code:
@@ -6,16 +6,16 @@ RSpec.describe "Portal::SalesController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant,
+      enterprise:,
+      tenant:,
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
-  let(:contact) { FactoryBot.create(:contact, :trait1,  tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }
+  let(:contact) { FactoryBot.create(:contact, :trait1,  tenant:) }

File: spec/requests/portal/users_spec.rb
Apply the fix to this code:
@@ -4,11 +4,11 @@ require "rails_helper"
 
-  let(:tenant) { create(:tenant, :trait1, enterprise: enterprise) }
-  let(:contact) { create(:contact, :trait1, tenant: tenant) }
-  let(:current_user) { create(:user, enterprise: enterprise, first_name: "Super", last_name: "User", tenant: tenant, role: "Super User", eula_accepted_at: Time.now) }
-  let(:parent_user1) { create(:user, first_name: "John", last_name: "Doe", role: "Portal User", enterprise: enterprise, tenant: tenant) }
-  let(:parent_user2) { create(:user, first_name: "Jane", last_name: "Smith", role: "Portal User", enterprise: enterprise, tenant: tenant) }
+  let(:tenant) { create(:tenant, :trait1, enterprise:) }
+  let(:contact) { create(:contact, :trait1, tenant:) }
+  let(:current_user) { create(:user, enterprise:, first_name: "Super", last_name: "User", tenant:, role: "Super User", eula_accepted_at: Time.now) }
+  let(:parent_user1) { create(:user, first_name: "John", last_name: "Doe", role: "Portal User", enterprise:, tenant:) }
+  let(:parent_user2) { create(:user, first_name: "Jane", last_name: "Smith", role: "Portal User", enterprise:, tenant:) }

File: spec/requests/reports_spec.rb
Apply the fix to this code:
@@ -10,8 +10,8 @@ RSpec.describe "ReportsController" , type: :request do
-      enterprise: enterprise,
-      tenant: tenant,
+      enterprise:,
+      tenant:,

Apply the fix to this code:
@@ -22,12 +22,12 @@ RSpec.describe "ReportsController" , type: :request do
 
-  let(:enterprise) { FactoryBot.create(:enterprise, :trait1, platform_type: platform_type) }
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
-  let(:estimate) { FactoryBot.create(:company, :trait1, tenant: tenant, company: company, created_at: 5.days.ago, updated_at: 5.days.ago) }
-  let(:order) { FactoryBot.create(:order, :trait1, tenant: tenant, created_at: 5.days.ago, updated_at: 5.days.ago) }
-  let(:sale) { FactoryBot.create(:sale, :trait1, tenant: tenant, created_at: 5.days.ago, updated_at: 5.days.ago) }
-  let(:company) { FactoryBot.create(:company, :trait1, tenant: tenant) }
+  let(:enterprise) { FactoryBot.create(:enterprise, :trait1, platform_type:) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }
+  let(:estimate) { FactoryBot.create(:company, :trait1, tenant:, company:, created_at: 5.days.ago, updated_at: 5.days.ago) }
+  let(:order) { FactoryBot.create(:order, :trait1, tenant:, created_at: 5.days.ago, updated_at: 5.days.ago) }
+  let(:sale) { FactoryBot.create(:sale, :trait1, tenant:, created_at: 5.days.ago, updated_at: 5.days.ago) }
+  let(:company) { FactoryBot.create(:company, :trait1, tenant:) }

Apply the fix to this code:
@@ -80,17 +80,17 @@ RSpec.describe "ReportsController" , type: :request do
-          sales_chart: OpenStruct.new(highchart_presenter: highchart_presenter),
-          shipment_by_macro_area_value_chart: OpenStruct.new(highchart_presenter: highchart_presenter),
-          shipment_by_macro_area_count_chart: OpenStruct.new(highchart_presenter: highchart_presenter),
-          sales_by_profit_chart: OpenStruct.new(highchart_presenter: highchart_presenter),
-          sales_by_profit_chart_ly: OpenStruct.new(highchart_presenter: highchart_presenter),
-          shipments_service_chart: OpenStruct.new(highchart_presenter: highchart_presenter),
-          shipments_courier_chart: OpenStruct.new(highchart_presenter: highchart_presenter),
-          regular_companies_monthly_chart: OpenStruct.new(highchart_presenter: highchart_presenter),
-          walk_in_companies_monthly_chart: OpenStruct.new(highchart_presenter: highchart_presenter),
+          sales_chart: OpenStruct.new(highchart_presenter:),
+          shipment_by_macro_area_value_chart: OpenStruct.new(highchart_presenter:),
+          shipment_by_macro_area_count_chart: OpenStruct.new(highchart_presenter:),
+          sales_by_profit_chart: OpenStruct.new(highchart_presenter:),
+          sales_by_profit_chart_ly: OpenStruct.new(highchart_presenter:),
+          shipments_service_chart: OpenStruct.new(highchart_presenter:),
+          shipments_courier_chart: OpenStruct.new(highchart_presenter:),
+          regular_companies_monthly_chart: OpenStruct.new(highchart_presenter:),
+          walk_in_companies_monthly_chart: OpenStruct.new(highchart_presenter:),
-          customer_composition_chart: OpenStruct.new(highchart_presenter: highchart_presenter),
+          customer_composition_chart: OpenStruct.new(highchart_presenter:),

File: spec/requests/shipments_spec.rb
Apply the fix to this code:
@@ -11,8 +11,8 @@ RSpec.describe "ShipmentsController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
