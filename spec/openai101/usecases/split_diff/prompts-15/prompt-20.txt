In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: app/services/corebridge/import/interactors/contact/upsert.rb
Apply the fix to this code:
@@ -16,11 +16,11 @@ class Corebridge::Import::Interactors::Contact::Upsert
-        contact: contact,
-        created: created,
-        modified: modified,
-        source: source,
-        account_source: account_source
+        contact:,
+        created:,
+        modified:,
+        source:,
+        account_source:

File: app/services/corebridge/import/interactors/sales_rep/upsert.rb
Apply the fix to this code:
@@ -13,10 +13,10 @@ class Corebridge::Import::Interactors::SalesRep::Upsert
-        sales_rep: sales_rep,
-        created: created,
-        modified: modified,
-        source: source
+        sales_rep:,
+        created:,
+        modified:,
+        source:

File: app/services/invoca/import/base.rb
Apply the fix to this code:
@@ -27,7 +27,7 @@ class Invoca::Import::Base
-    data = import_api.last_transaction(last_date, last_transaction, size: size)
+    data = import_api.last_transaction(last_date, last_transaction, size:)

File: app/services/invoca/import/interactors/phone_call_data/upsert.rb
Apply the fix to this code:
@@ -41,7 +41,7 @@ class Invoca::Import::Interactors::PhoneCallData::Upsert
-      phone_number: phone_number,
+      phone_number:,

File: app/services/mbe/api/base.rb
Apply the fix to this code:
@@ -34,8 +34,8 @@ class Mbe::Api::Base
 
-      method: method,
-      request_url: request_url,
+      method:,
+      request_url:,

Apply the fix to this code:
@@ -127,7 +127,7 @@ class Mbe::Api::Base
-        curl: curl
+        curl:

Apply the fix to this code:
@@ -182,7 +182,7 @@ class Mbe::Api::Base
 
-      error_class: error_class,
+      error_class:,

File: app/services/mbe/api/customer.rb
Apply the fix to this code:
@@ -11,8 +11,8 @@ class Mbe::Api::Customer < Mbe::Api::Base
-      page: page,
-      size: size,
+      page:,
+      size:,

Apply the fix to this code:
@@ -53,7 +53,7 @@ class Mbe::Api::Customer < Mbe::Api::Base
 
-    request("customers/#{customer_id}", method: :POST, user: user, json: {
+    request("customers/#{customer_id}", method: :POST, user:, json: {

Apply the fix to this code:
@@ -106,23 +106,23 @@ class Mbe::Api::Customer < Mbe::Api::Base
 
-    request("customers/#{customer_id}/contacts", method: :POST, user: user, json: contact_data)
+    request("customers/#{customer_id}/contacts", method: :POST, user:, json: contact_data)
 
-    request("customers/#{customer_id}/contacts/#{contact_id}", method: :PUT, user: user, json: contact_data)
+    request("customers/#{customer_id}/contacts/#{contact_id}", method: :PUT, user:, json: contact_data)
 
-    request("customers/#{customer_id}/contacts/#{contact_id}", method: :DELETE, user: user)
+    request("customers/#{customer_id}/contacts/#{contact_id}", method: :DELETE, user:)
 
-    request("shipments/gdpr/createAssociation2", method: :POST, user: user, json: privacy_data)
+    request("shipments/gdpr/createAssociation2", method: :POST, user:, json: privacy_data)
 
-    request("customers/#{customer_id}/agents", method: :POST, user: user, json: {
+    request("customers/#{customer_id}/agents", method: :POST, user:, json: {

File: app/services/mbe/api/offer.rb
Apply the fix to this code:
@@ -3,8 +3,8 @@ class Mbe::Api::Offer < Mbe::Api::Base
 
-      page: page,
-      size: size,
+      page:,
+      size:,

Apply the fix to this code:
@@ -37,7 +37,7 @@ class Mbe::Api::Offer < Mbe::Api::Base
 
-      path: path,
+      path:,

File: app/services/mbe/api/royalty.rb
Apply the fix to this code:
@@ -3,8 +3,8 @@ class Mbe::Api::Royalty < Mbe::Api::Base
-      year: year,
-      month: month,
+      year:,
+      month:,

File: app/services/mbe/api/user.rb
Apply the fix to this code:
@@ -10,8 +10,8 @@ class Mbe::Api::User < Mbe::Api::Base
-        page: page,
-        size: size,
+        page:,
+        size:,

Apply the fix to this code:
@@ -28,13 +28,13 @@ class Mbe::Api::User < Mbe::Api::Base
 
-    search(timestamp: timestamp, page: page, size: size)
+    search(timestamp:, page:, size:)
 
 
-    data = search(username: username, size: 200)
+    data = search(username:, size: 200)

File: app/services/mbe/export/base.rb
Apply the fix to this code:
@@ -57,7 +57,7 @@ class Mbe::Export::Base
 
-      error_class: error_class,
+      error_class:,

File: app/services/mbe/import/base.rb
Apply the fix to this code:
@@ -66,7 +66,7 @@ class Mbe::Import::Base
 
-    data = import_api.last_updated(last_updated_at, page: page, size: size)
+    data = import_api.last_updated(last_updated_at, page:, size:)

Apply the fix to this code:
@@ -77,7 +77,7 @@ class Mbe::Import::Base
-            context_source: context_source
+            context_source:

Apply the fix to this code:
@@ -234,7 +234,7 @@ class Mbe::Import::Base
 
-      error_class: error_class,
+      error_class:,

File: app/services/mbe/import/budget.rb
Apply the fix to this code:
@@ -5,7 +5,7 @@ class Mbe::Import::Budget < Mbe::Import::Base
-    budget = ::Budget.where(tenant_id: @tenant.id, financial_year: financial_year).first
+    budget = ::Budget.where(tenant_id: @tenant.id, financial_year:).first


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
