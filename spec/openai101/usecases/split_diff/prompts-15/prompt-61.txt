In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: spec/services/printsmith/import/interactors/invoice/update_spec.rb
Apply the fix to this code:
@@ -4,12 +4,12 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, :api, enterprise: enterprise) }
-  let(:company) { FactoryBot.create(:company, :trait1, tenant: tenant) }
-  let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, :api, enterprise:) }
+  let(:company) { FactoryBot.create(:company, :trait1, tenant:) }
+  let(:contact) { FactoryBot.create(:contact, :trait1, tenant:) }
-  let!(:invoice) { FactoryBot.create(:invoice, :trait1, tenant: tenant, sale_id: nil) }
+  let!(:invoice) { FactoryBot.create(:invoice, :trait1, tenant:, sale_id: nil) }

Apply the fix to this code:
@@ -19,8 +19,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
 
-          invoice: invoice,
-          tenant: tenant,
+          invoice:,
+          tenant:,

Apply the fix to this code:
@@ -31,8 +31,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-              invoice: invoice,
-              tenant: tenant,
+              invoice:,
+              tenant:,

Apply the fix to this code:
@@ -48,8 +48,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-                invoice: invoice,
-                tenant: tenant,
+                invoice:,
+                tenant:,

Apply the fix to this code:
@@ -62,8 +62,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-                invoice: invoice,
-                tenant: tenant,
+                invoice:,
+                tenant:,

Apply the fix to this code:
@@ -79,8 +79,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-                invoice: invoice,
-                tenant: tenant,
+                invoice:,
+                tenant:,

Apply the fix to this code:
@@ -93,8 +93,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-                invoice: invoice,
-                tenant: tenant,
+                invoice:,
+                tenant:,

Apply the fix to this code:
@@ -112,7 +112,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
 
-            subject.execute(invoice: invoice, tenant: tenant)
+            subject.execute(invoice:, tenant:)

Apply the fix to this code:
@@ -126,7 +126,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
 
-            subject.execute(invoice: invoice, tenant: tenant)
+            subject.execute(invoice:, tenant:)

Apply the fix to this code:
@@ -135,7 +135,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-      let(:invoice) { FactoryBot.build(:invoice, :trait1, tenant: tenant) }
+      let(:invoice) { FactoryBot.build(:invoice, :trait1, tenant:) }

File: spec/services/printsmith/import/interactors/production_location/upsert_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:production_location) { FactoryBot.build(:production_location, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:production_location) { FactoryBot.build(:production_location, :trait2, tenant:) }

Apply the fix to this code:
@@ -16,10 +16,10 @@ RSpec.describe Printsmith::Import::Interactors::ProductionLocation::Upsert, type
-          production_location: production_location,
-          created: created,
-          modified: modified,
-          source: source
+          production_location:,
+          created:,
+          modified:,
+          source:

File: spec/services/printsmith/import/interactors/sales_base_tax/upsert_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:sales_base_tax) { FactoryBot.build(:sales_base_tax, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:sales_base_tax) { FactoryBot.build(:sales_base_tax, :trait2, tenant:) }

Apply the fix to this code:
@@ -18,10 +18,10 @@ RSpec.describe Printsmith::Import::Interactors::SalesBaseTax::Upsert, type: :ser
-            sales_base_tax: sales_base_tax,
-            created: created,
-            modified: modified,
-            source: source,
+            sales_base_tax:,
+            created:,
+            modified:,
+            source:,

Apply the fix to this code:
@@ -33,14 +33,14 @@ RSpec.describe Printsmith::Import::Interactors::SalesBaseTax::Upsert, type: :ser
 
-        let(:sales_base_tax_existing) { FactoryBot.create(:sales_base_tax, :trait2, tenant: tenant) }
+        let(:sales_base_tax_existing) { FactoryBot.create(:sales_base_tax, :trait2, tenant:) }
 
-            created: created,
-            modified: modified,
-            source: source,
+            created:,
+            modified:,
+            source:,

File: spec/services/printsmith/import/interactors/sales_rep/upsert_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:sales_rep) { FactoryBot.build(:sales_rep, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:sales_rep) { FactoryBot.build(:sales_rep, :trait2, tenant:) }

Apply the fix to this code:
@@ -16,10 +16,10 @@ RSpec.describe Printsmith::Import::Interactors::SalesRep::Upsert, type: :service
-          sales_rep: sales_rep,
-          created: created,
-          modified: modified,
-          source: source
+          sales_rep:,
+          created:,
+          modified:,
+          source:

File: spec/services/printsmith/import/interactors/sales_summary/upsert_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:sales_summary) { FactoryBot.build(:sales_summary, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:sales_summary) { FactoryBot.build(:sales_summary, :trait2, tenant:) }

Apply the fix to this code:
@@ -18,10 +18,10 @@ RSpec.describe Printsmith::Import::Interactors::SalesSummary::Upsert, type: :ser
-            sales_summary: sales_summary,
-            created: created,
-            modified: modified,
-            source: source,
+            sales_summary:,
+            created:,
+            modified:,
+            source:,

Apply the fix to this code:
@@ -34,14 +34,14 @@ RSpec.describe Printsmith::Import::Interactors::SalesSummary::Upsert, type: :ser
 
-        let(:sales_summary_existing) { FactoryBot.create(:sales_summary, :trait2, tenant: tenant, daily: true, monthly: false) }
+        let(:sales_summary_existing) { FactoryBot.create(:sales_summary, :trait2, tenant:, daily: true, monthly: false) }
 
-            created: created,
-            modified: modified,
-            source: source,
+            created:,
+            modified:,
+            source:,


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
