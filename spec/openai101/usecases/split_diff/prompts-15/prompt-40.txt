In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: spec/requests/business_plan_plan_items_controller_spec.rb
Apply the fix to this code:
@@ -12,15 +12,15 @@ RSpec.describe BusinessPlanPlanItemsController, type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant:) }
-  let(:business_plan_plan_item) { FactoryBot.create(:business_plan_plan_item, :trait1, business_plan: business_plan, business_plan_plan_category_id: business_plan_plan_category.id) }
+  let(:business_plan_plan_item) { FactoryBot.create(:business_plan_plan_item, :trait1, business_plan:, business_plan_plan_category_id: business_plan_plan_category.id) }

Apply the fix to this code:
@@ -74,7 +74,7 @@ RSpec.describe BusinessPlanPlanItemsController, type: :request do
 
-            post "/business_plans/#{business_plan.id}/business_plan_plan_items", params: params
+            post("/business_plans/#{business_plan.id}/business_plan_plan_items", params:)

File: spec/requests/business_plans_spec.rb
Apply the fix to this code:
@@ -12,13 +12,13 @@ RSpec.describe BusinessPlansController, type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:business_plan) { FactoryBot.create(:business_plan, :trait1, tenant:) }

Apply the fix to this code:
@@ -91,7 +91,7 @@ RSpec.describe BusinessPlansController, type: :request do
 
-            post "/business_plans", params: params
+            post("/business_plans", params:)

Apply the fix to this code:
@@ -143,8 +143,8 @@ RSpec.describe BusinessPlansController, type: :request do
 
-      let!(:business_plan_prev) { FactoryBot.create(:business_plan, :trait1, tenant: tenant, financial_year: business_plan.financial_year - 1) }
-      let!(:business_plan_sales_investment) { FactoryBot.create(:business_plan_sales_investment, :trait1, business_plan: business_plan_prev, tenant: tenant) }
+      let!(:business_plan_prev) { FactoryBot.create(:business_plan, :trait1, tenant:, financial_year: business_plan.financial_year - 1) }
+      let!(:business_plan_sales_investment) { FactoryBot.create(:business_plan_sales_investment, :trait1, business_plan: business_plan_prev, tenant:) }

File: spec/requests/callbacks_spec.rb
Apply the fix to this code:
@@ -11,17 +11,17 @@ RSpec.describe CallbacksController, type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant, mobile: "1235") }
-  let(:phone_call) { FactoryBot.create(:phone_call, :trait1, tenant: tenant, phoneable_id: contact.id, phoneable_type: "Contact", twillio_message_sid: "1234") }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:contact) { FactoryBot.create(:contact, :trait1, tenant:, mobile: "1235") }
+  let(:phone_call) { FactoryBot.create(:phone_call, :trait1, tenant:, phoneable_id: contact.id, phoneable_type: "Contact", twillio_message_sid: "1234") }

File: spec/requests/campaign_exclusions_spec.rb
Apply the fix to this code:
@@ -11,8 +11,8 @@ RSpec.describe "CampaignExclusionsController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:

Apply the fix to this code:
@@ -23,12 +23,12 @@ RSpec.describe "CampaignExclusionsController", type: :request do
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
-  let(:contact_trait1) { FactoryBot.create(:contact, :trait1, inquiry: inquiry_trait1, location_user_id: location_trait1.id, sales_rep_user_id: user_trait1.id, address: address_trait1, company: company_trait1, lead_type: lead_type_trait1, parent_contact: nil , prospect_status: prospect_status_trait1, tenant: tenant) }
-  let(:company_trait1) { FactoryBot.build(:company, :trait1, tenant: tenant, location_user_id: location_trait1.id) } ## , location: location_trait1) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }
+  let(:contact_trait1) { FactoryBot.create(:contact, :trait1, inquiry: inquiry_trait1, location_user_id: location_trait1.id, sales_rep_user_id: user_trait1.id, address: address_trait1, company: company_trait1, lead_type: lead_type_trait1, parent_contact: nil , prospect_status: prospect_status_trait1, tenant:) }
+  let(:company_trait1) { FactoryBot.build(:company, :trait1, tenant:, location_user_id: location_trait1.id) } ## , location: location_trait1) }
-  let(:lead_type_trait1) { FactoryBot.build(:lead_type, :trait1, tenant: tenant, enterprise: enterprise) }
-  let(:prospect_status_trait1) { FactoryBot.build(:prospect_status, :trait1, enterprise: enterprise) }
+  let(:lead_type_trait1) { FactoryBot.build(:lead_type, :trait1, tenant:, enterprise:) }
+  let(:prospect_status_trait1) { FactoryBot.build(:prospect_status, :trait1, enterprise:) }

File: spec/requests/cases_spec.rb
Apply the fix to this code:
@@ -4,10 +4,10 @@ RSpec.describe CasesController, type: :request do
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
-  let(:company) { FactoryBot.create(:company, :trait1, tenant: tenant) }
-  let(:user) { FactoryBot.create(:user, :trait1, tenant: tenant, enterprise: enterprise) }
-  let(:kase) { FactoryBot.create(:case, :trait1, tenant: tenant, company: company, user_id: user.id) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }
+  let(:company) { FactoryBot.create(:company, :trait1, tenant:) }
+  let(:user) { FactoryBot.create(:user, :trait1, tenant:, enterprise:) }
+  let(:kase) { FactoryBot.create(:case, :trait1, tenant:, company:, user_id: user.id) }

Apply the fix to this code:
@@ -54,7 +54,7 @@ RSpec.describe CasesController, type: :request do
 
-          post "/cases", params: params
+          post("/cases", params:)

File: spec/requests/email_aliases_spec.rb
Apply the fix to this code:
@@ -12,12 +12,12 @@ RSpec.describe EmailAliasesController, type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

File: spec/requests/email_configuration_spec.rb
Apply the fix to this code:
@@ -12,12 +12,12 @@ RSpec.describe EmailConfigurationController, type: :request do
-      tenant: tenant
+      tenant:
-  let!(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let!(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

Apply the fix to this code:
@@ -68,7 +68,7 @@ RSpec.describe EmailConfigurationController, type: :request do
 
-            post "/email_configuration", params: params
+            post("/email_configuration", params:)

Apply the fix to this code:
@@ -99,7 +99,7 @@ RSpec.describe EmailConfigurationController, type: :request do
 
-            put "/email_configuration/#{signin_user.id}", params: params
+            put("/email_configuration/#{signin_user.id}", params:)

File: spec/requests/email_templates_spec.rb
Apply the fix to this code:
@@ -12,13 +12,13 @@ RSpec.describe EmailTemplatesController, type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:email_template) { FactoryBot.create(:email_template, :trait2, tenant: tenant, enterprise_id: tenant.enterprise.id) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:email_template) { FactoryBot.create(:email_template, :trait2, tenant:, enterprise_id: tenant.enterprise.id) }

File: spec/requests/enterprises/assets_spec.rb
Apply the fix to this code:
@@ -63,7 +63,7 @@ RSpec.describe "Enterprises::AssetsController", type: :request do
 
-            post "/enterprises/assets", params: params
+            post("/enterprises/assets", params:)

File: spec/requests/enterprises/business_plan_goals_spec.rb
Apply the fix to this code:
@@ -55,7 +55,7 @@ RSpec.describe "Enterprises::BusinessPlanGoalsController", type: :request do
 
-            post "/enterprises/business_plan_goals", params: params
+            post("/enterprises/business_plan_goals", params:)

File: spec/requests/enterprises/business_plan_kpis_spec.rb
Apply the fix to this code:
@@ -46,7 +46,7 @@ RSpec.describe "Enterprises::BusinessPlanKpisController", type: :request do
 
-            post enterprises_business_plan_kpis_path(), params: params
+            post(enterprises_business_plan_kpis_path(), params:)

File: spec/requests/enterprises/business_plan_marketing_activities_spec.rb
Apply the fix to this code:
@@ -18,7 +18,7 @@ RSpec.describe "Enterprises::BusinessPlanMarketingActivitiesController", type: :
 
-  let(:business_plan_marketing_activity) { FactoryBot.create(:business_plan_marketing_activity, :trait1, business_plan: business_plan, enterprise: enterprise_trait3) }
+  let(:business_plan_marketing_activity) { FactoryBot.create(:business_plan_marketing_activity, :trait1, business_plan:, enterprise: enterprise_trait3) }

Apply the fix to this code:
@@ -48,7 +48,7 @@ RSpec.describe "Enterprises::BusinessPlanMarketingActivitiesController", type: :
 
-            post enterprises_business_plan_marketing_activities_path(), params: params
+            post(enterprises_business_plan_marketing_activities_path(), params:)


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
