In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: app/models/sales_summary.rb
Apply the fix to this code:
@@ -80,7 +80,7 @@ class SalesSummary < ApplicationRecord
 
-      select_invoices_query = ActiveRecord::Base.send(:sanitize_sql, [select_invoices_query, tenant_id: tenant_id, start_date: start_date, end_date: end_date])
+      select_invoices_query = ActiveRecord::Base.send(:sanitize_sql, [select_invoices_query, tenant_id:, start_date:, end_date:])

Apply the fix to this code:
@@ -93,7 +93,7 @@ class SalesSummary < ApplicationRecord
 
-      select_invoices_query = ActiveRecord::Base.send(:sanitize_sql, [select_invoices_query, tenant_id: tenant_id, start_date: start_date, end_date: end_date])
+      select_invoices_query = ActiveRecord::Base.send(:sanitize_sql, [select_invoices_query, tenant_id:, start_date:, end_date:])

Apply the fix to this code:
@@ -130,7 +130,7 @@ class SalesSummary < ApplicationRecord
 
-    select_shipments_query = ActiveRecord::Base.send(:sanitize_sql, [select_shipments_query, tenant_id: tenant_id, start_date: start_date, end_date: end_date])
+    select_shipments_query = ActiveRecord::Base.send(:sanitize_sql, [select_shipments_query, tenant_id:, start_date:, end_date:])

Apply the fix to this code:
@@ -701,11 +701,11 @@ class SalesSummary < ApplicationRecord
-          tenant: tenant,
+          tenant:,
-          accounting_month: accounting_month,
-          accounting_year: accounting_year
+          accounting_month:,
+          accounting_year:

Apply the fix to this code:
@@ -769,7 +769,7 @@ class SalesSummary < ApplicationRecord
 
-      old_stat_ids = Statistic.where(tenant: tenant, statistic_for: "PS-DAY", accounting_month: accounting_month, accounting_year: accounting_year).where.not(id: daily_stat_ids).pluck(:id)
+      old_stat_ids = Statistic.where(tenant:, statistic_for: "PS-DAY", accounting_month:, accounting_year:).where.not(id: daily_stat_ids).pluck(:id)

Apply the fix to this code:
@@ -793,10 +793,10 @@ class SalesSummary < ApplicationRecord
 
-        tenant: tenant,
+        tenant:,
-        accounting_year: accounting_year,
-        accounting_month: accounting_month
+        accounting_year:,
+        accounting_month:

File: app/models/scopes/company_scopes.rb
Apply the fix to this code:
@@ -99,11 +99,11 @@ module Scopes
 
-      scope :single_purchase_in_daterange, -> (start_date, end_date, invoiced: false) { any_sale_in_daterange(start_date, end_date, invoiced: invoiced).where("(#{Sale.select("COUNT(invoices.id)").invoiced(invoiced).where("invoices.tenant_id = companies.tenant_id").where("invoices.company_id = companies.id").where(pickup_date: start_date..end_date).to_sql}) = 1") }
-      scope :multiple_purchase_in_daterange, -> (start_date, end_date, invoiced: false) { any_sale_in_daterange(start_date, end_date, invoiced: invoiced).where("(#{Sale.select("COUNT(invoices.id)").invoiced(invoiced).where("invoices.tenant_id = companies.tenant_id").where("invoices.company_id = companies.id").where(pickup_date: start_date..end_date).to_sql}) > 1") }
+      scope :single_purchase_in_daterange, -> (start_date, end_date, invoiced: false) { any_sale_in_daterange(start_date, end_date, invoiced:).where("(#{Sale.select("COUNT(invoices.id)").invoiced(invoiced).where("invoices.tenant_id = companies.tenant_id").where("invoices.company_id = companies.id").where(pickup_date: start_date..end_date).to_sql}) = 1") }
+      scope :multiple_purchase_in_daterange, -> (start_date, end_date, invoiced: false) { any_sale_in_daterange(start_date, end_date, invoiced:).where("(#{Sale.select("COUNT(invoices.id)").invoiced(invoiced).where("invoices.tenant_id = companies.tenant_id").where("invoices.company_id = companies.id").where(pickup_date: start_date..end_date).to_sql}) > 1") }
-      scope :best_similarity_value, -> (name, min_similarity=0.3) { select(ActiveRecord::Base.send(:sanitize_sql, ["word_similarity(:name, companies.name) AS best_similarity", name: name])).where("word_similarity(?, companies.name) >= ?", name, min_similarity).order("1 DESC").limit(1).try(:first).try(:best_similarity).to_f }
+      scope :best_similarity_value, -> (name, min_similarity=0.3) { select(ActiveRecord::Base.send(:sanitize_sql, ["word_similarity(:name, companies.name) AS best_similarity", name:])).where("word_similarity(?, companies.name) >= ?", name, min_similarity).order("1 DESC").limit(1).try(:first).try(:best_similarity).to_f }

Apply the fix to this code:
@@ -132,7 +132,7 @@ module Scopes
 
-        statuses.include?(status) ? where(status: status) : all
+        statuses.include?(status) ? where(status:) : all

Apply the fix to this code:
@@ -256,7 +256,7 @@ module Scopes
-              where(industry: industry)
+              where(industry:)

Apply the fix to this code:
@@ -270,12 +270,12 @@ module Scopes
 
-        size == "N/A" ? where(size: ["N/A", nil]) : where(size: size)
+        size == "N/A" ? where(size: ["N/A", nil]) : where(size:)
 
-        profitability == "N/A" ? where(profitability: ["N/A", nil]) : where(profitability: profitability)
+        profitability == "N/A" ? where(profitability: ["N/A", nil]) : where(profitability:)

Apply the fix to this code:
@@ -283,11 +283,11 @@ module Scopes
 
-        by_field_date_range(field: "last_contact", filter: filter)
+        by_field_date_range(field: "last_contact", filter:)
 
-        by_field_date_range(field: "last_pickup_date", filter: filter)
+        by_field_date_range(field: "last_pickup_date", filter:)

Apply the fix to this code:
@@ -316,11 +316,11 @@ module Scopes
 
-        joins(:bookmarks).where(bookmarks: {user_id: user_id})
+        joins(:bookmarks).where(bookmarks: {user_id:})
 
-        joins(:exclusions).where(exclusions: {user_id: user_id})
+        joins(:exclusions).where(exclusions: {user_id:})

File: app/models/shipment.rb
Apply the fix to this code:
@@ -23,12 +23,12 @@ class Shipment < ApplicationRecord
 
-  scope :standard, -> (tenant) { where(tenant: tenant, status: tenant.standard_shipments) }
-  scope :drafts, -> (tenant) { where(tenant: tenant, status: tenant.draft_shipments) }
+  scope :standard, -> (tenant) { where(tenant:, status: tenant.standard_shipments) }
+  scope :drafts, -> (tenant) { where(tenant:, status: tenant.draft_shipments) }
-  scope :macro_area_destination, -> (macro_area) { where("shipments.platform_data->>'macro_area' = :macro_area", macro_area: macro_area) }
+  scope :macro_area_destination, -> (macro_area) { where("shipments.platform_data->>'macro_area' = :macro_area", macro_area:) }

Apply the fix to this code:
@@ -126,12 +126,12 @@ class Shipment < ApplicationRecord
 
-    Invoice.find_by(tenant: tenant, platform_id: source_invoice_platform_id) if source_invoice_platform_id.present?
+    Invoice.find_by(tenant:, platform_id: source_invoice_platform_id) if source_invoice_platform_id.present?
 
-    Invoice.where(tenant: tenant).where("platform_data->>'source_shipment_platform_id' = ?", platform_id).reorder("").take
+    Invoice.where(tenant:).where("platform_data->>'source_shipment_platform_id' = ?", platform_id).reorder("").take

Apply the fix to this code:
@@ -174,7 +174,7 @@ class Shipment < ApplicationRecord
-          "#{ApplicationController.helpers.number_to_currency(shipment.grand_total, tenant: tenant, precision: 2)}"
+          "#{ApplicationController.helpers.number_to_currency(shipment.grand_total, tenant:, precision: 2)}"

Apply the fix to this code:
@@ -182,8 +182,8 @@ class Shipment < ApplicationRecord
 
-      tenant: tenant,
-      user: user,
+      tenant:,
+      user:,

File: app/models/statistic.rb
Apply the fix to this code:
@@ -18,7 +18,7 @@ class Statistic < ApplicationRecord
 
-      tenant: tenant,
+      tenant:,

Apply the fix to this code:
@@ -29,9 +29,9 @@ class Statistic < ApplicationRecord
-        tenant: tenant,
-        user: user,
-        statistic_for: statistic_for,
+        tenant:,
+        user:,
+        statistic_for:,

Apply the fix to this code:
@@ -40,9 +40,9 @@ class Statistic < ApplicationRecord
 
-          tenant: tenant,
+          tenant:,
-          statistic_for: statistic_for,
+          statistic_for:,

Apply the fix to this code:
@@ -56,9 +56,9 @@ class Statistic < ApplicationRecord
 
-          tenant: tenant,
-          location: location,
-          statistic_for: statistic_for,
+          tenant:,
+          location:,
+          statistic_for:,


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
