In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: spec/requests/enterprises/users_spec.rb
Apply the fix to this code:
@@ -63,7 +63,7 @@ RSpec.describe "Enterprises::UsersController", type: :request do
 
-            post "/enterprises/users", params: params
+            post("/enterprises/users", params:)

File: spec/requests/enterprises_spec.rb
Apply the fix to this code:
@@ -18,17 +18,17 @@ RSpec.describe EnterprisesController, type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
-  let!(:group) { FactoryBot.create(:group, :trait1, enterprise: enterprise, user: signin_user) }
-  let!(:group_tenant) { GroupsTenant.create(tenant: tenant, group: group) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }
+  let!(:group) { FactoryBot.create(:group, :trait1, enterprise:, user: signin_user) }
+  let!(:group_tenant) { GroupsTenant.create(tenant:, group:) }

Apply the fix to this code:
@@ -159,7 +159,7 @@ RSpec.describe EnterprisesController, type: :request do
 
-          post "/enterprises/reports/csv_table", params: params
+          post("/enterprises/reports/csv_table", params:)

File: spec/requests/estimates_spec.rb
Apply the fix to this code:
@@ -5,7 +5,7 @@ RSpec.describe "EstimatesController", type: :request do
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, :api, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, :api, enterprise:) }

Apply the fix to this code:
@@ -13,8 +13,8 @@ RSpec.describe "EstimatesController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:

File: spec/requests/exclusions_spec.rb
Apply the fix to this code:
@@ -11,16 +11,16 @@ RSpec.describe ExclusionsController, type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
-  let(:company) { FactoryBot.create(:company, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }
+  let(:company) { FactoryBot.create(:company, :trait1, tenant:) }

Apply the fix to this code:
@@ -40,7 +40,7 @@ RSpec.describe ExclusionsController, type: :request do
 
-            post "/exclusions", params: params
+            post("/exclusions", params:)

File: spec/requests/holidays_spec.rb
Apply the fix to this code:
@@ -11,14 +11,14 @@ RSpec.describe "HolidaysController", type: :request do
-      tenant: tenant
+      tenant:
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:holiday) { FactoryBot.build(:holiday, :trait1, tenant: tenant, enterprise: tenant.enterprise, user: signin_user) }
-  let(:holiday_date) { FactoryBot.create(:holiday_date, :trait1, holiday: holiday) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:holiday) { FactoryBot.build(:holiday, :trait1, tenant:, enterprise: tenant.enterprise, user: signin_user) }
+  let(:holiday_date) { FactoryBot.create(:holiday_date, :trait1, holiday:) }

Apply the fix to this code:
@@ -98,7 +98,7 @@ RSpec.describe "HolidaysController", type: :request do
 
-            post "/holidays", params: params
+            post("/holidays", params:)

File: spec/requests/inquiries_spec.rb
Apply the fix to this code:
@@ -5,12 +5,12 @@ RSpec.describe InquiriesController, type: :request do
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, tenant_picker_name: "Test-1") }
-  let(:another_tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, tenant_picker_name: "Test-2") }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, tenant_picker_name: "Test-1") }
+  let(:another_tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, tenant_picker_name: "Test-2") }
 
-  let!(:inquiry) { FactoryBot.create(:inquiry, :trait1, company: company_trait1, contact: contact_trait1, tenant: tenant, user: user_trait1) }
-  let(:contact_trait1) { FactoryBot.create(:contact, :trait1, address: address_trait1, company: company_trait1, tenant: tenant) }
-  let(:company_trait1) { FactoryBot.create(:company, :trait1, tenant: tenant) }
+  let!(:inquiry) { FactoryBot.create(:inquiry, :trait1, company: company_trait1, contact: contact_trait1, tenant:, user: user_trait1) }
+  let(:contact_trait1) { FactoryBot.create(:contact, :trait1, address: address_trait1, company: company_trait1, tenant:) }
+  let(:company_trait1) { FactoryBot.create(:company, :trait1, tenant:) }

Apply the fix to this code:
@@ -18,9 +18,9 @@ RSpec.describe InquiriesController, type: :request do
-      token: token,
-      enterprise: enterprise,
-      tenant: tenant,
+      token:,
+      enterprise:,
+      tenant:,

Apply the fix to this code:
@@ -67,7 +67,7 @@ RSpec.describe InquiriesController, type: :request do
 
-          get "/inquiries/#{inquiry.id}/update_assigned_user", params: params
+          get("/inquiries/#{inquiry.id}/update_assigned_user", params:)

File: spec/requests/leads_dashboard_spec.rb
Apply the fix to this code:
@@ -5,8 +5,8 @@ RSpec.describe "LeadsController", type: :request do
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait3, enterprise: enterprise) }
-  let!(:sales_summary) { FactoryBot.create(:sales_summary, :trait1, monthly: true, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait3, enterprise:) }
+  let!(:sales_summary) { FactoryBot.create(:sales_summary, :trait1, monthly: true, tenant:) }

Apply the fix to this code:
@@ -14,9 +14,9 @@ RSpec.describe "LeadsController", type: :request do
-      token: token,
-      enterprise: enterprise,
-      tenant: tenant
+      token:,
+      enterprise:,
+      tenant:

File: spec/requests/notes_spec.rb
Apply the fix to this code:
@@ -10,22 +10,22 @@ RSpec.describe "NotesController", type: :request do
-      enterprise: enterprise,
-      tenant: tenant
+      enterprise:,
+      tenant:
 
-  let(:enterprise) { FactoryBot.create(:enterprise, :trait1, platform_type: platform_type) }
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:enterprise) { FactoryBot.create(:enterprise, :trait1, platform_type:) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
 
-  let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant: tenant) }
-  let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant) }
-  let(:note_trait1) { FactoryBot.create(:note, :trait1, tenant: tenant, context_id: estimate.id, context_type: "Estimate", user_id: sales_rep.id) }
-  let(:note_trait2) { FactoryBot.create(:note, :trait2, tenant: tenant, context_id: contact.id, context_type: "Contact", user_id: sales_rep.id) }
-  let(:sales_rep) { FactoryBot.create(:sales_rep, :trait1, tenant: tenant, platform_id: "test") }
+  let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant:) }
+  let(:contact) { FactoryBot.create(:contact, :trait1, tenant:) }
+  let(:note_trait1) { FactoryBot.create(:note, :trait1, tenant:, context_id: estimate.id, context_type: "Estimate", user_id: sales_rep.id) }
+  let(:note_trait2) { FactoryBot.create(:note, :trait2, tenant:, context_id: contact.id, context_type: "Contact", user_id: sales_rep.id) }
+  let(:sales_rep) { FactoryBot.create(:sales_rep, :trait1, tenant:, platform_id: "test") }

Apply the fix to this code:
@@ -85,14 +85,14 @@ RSpec.describe "NotesController", type: :request do
 
-          post "/notes", params: params
+          post("/notes", params:)
 
-        let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant: tenant) }
+        let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant:) }

File: spec/requests/passwords_spec.rb
Apply the fix to this code:
@@ -4,10 +4,10 @@ RSpec.describe "PasswordsController", type: :request do
 
-  let(:tenant) { create(:tenant, :trait1, enterprise: enterprise) }
+  let(:tenant) { create(:tenant, :trait1, enterprise:) }
-    create(:user, :trait3, *user_traits, **user_values, role: "Portal User", reset_password_sent_at: Time.now, username: "test@printspeak.com", manual_email: "test@printspeak.com", reset_password_token: token, enterprise: enterprise, tenant: tenant)
+    create(:user, :trait3, *user_traits, **user_values, role: "Portal User", reset_password_sent_at: Time.now, username: "test@printspeak.com", manual_email: "test@printspeak.com", reset_password_token: token, enterprise:, tenant:)

File: spec/requests/phone_call_data_spec.rb
Apply the fix to this code:
@@ -17,9 +17,9 @@ RSpec.describe "PhoneCallDataController", type: :request do
-  let(:tenant) { FactoryBot.create(:tenant, enterprise: enterprise) }
-  let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant) }
-  let!(:phone_call_data) { FactoryBot.create(:phone_call_data, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, enterprise:) }
+  let(:contact) { FactoryBot.create(:contact, :trait1, tenant:) }
+  let!(:phone_call_data) { FactoryBot.create(:phone_call_data, :trait1, tenant:) }

Apply the fix to this code:
@@ -44,7 +44,7 @@ RSpec.describe "PhoneCallDataController", type: :request do
-        let(:estimate) { FactoryBot.create(:estimate, tenant: tenant, contact_id: contact.id, deleted: false, voided: false) }
+        let(:estimate) { FactoryBot.create(:estimate, tenant:, contact_id: contact.id, deleted: false, voided: false) }


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
