In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: app/services/printsmith/export/estimate.rb
Apply the fix to this code:
@@ -25,7 +25,7 @@ class Printsmith::Export::Estimate < Printsmith::Export::Base
-          body: body
+          body:

File: app/services/printsmith/export/invoice.rb
Apply the fix to this code:
@@ -24,7 +24,7 @@ class Printsmith::Export::Invoice < Printsmith::Export::Base
-          body: body
+          body:

File: app/services/printsmith/import/association_resolver.rb
Apply the fix to this code:
@@ -10,7 +10,7 @@ class Printsmith::Import::AssociationResolver
 
-      klass.unscoped.where(tenant: tenant).where(associations_complete: false).order(created_at: :desc).limit(200).each do |resource|
+      klass.unscoped.where(tenant:).where(associations_complete: false).order(created_at: :desc).limit(200).each do |resource|

Apply the fix to this code:
@@ -25,7 +25,7 @@ class Printsmith::Import::AssociationResolver
 
-    klass.unscoped.where(tenant: tenant).where(id: id).each do |resource|
+    klass.unscoped.where(tenant:).where(id:).each do |resource|

File: app/services/printsmith/import/base.rb
Apply the fix to this code:
@@ -59,7 +59,7 @@ class Printsmith::Import::Base
 
-    data = import_api.last_updated(last_updated_at, offset: current_offset, size: size)
+    data = import_api.last_updated(last_updated_at, offset: current_offset, size:)

Apply the fix to this code:
@@ -73,7 +73,7 @@ class Printsmith::Import::Base
 
-        data: data,
+        data:,

Apply the fix to this code:
@@ -149,11 +149,11 @@ class Printsmith::Import::Base
-        last_updated_at: last_updated_at,
-        last_context: last_context,
-        last_result: last_result,
-        current_timestamp: current_timestamp,
-        records: records
+        last_updated_at:,
+        last_context:,
+        last_result:,
+        current_timestamp:,
+        records:

File: app/services/printsmith/import/company_ranking.rb
Apply the fix to this code:
@@ -20,8 +20,8 @@ class Printsmith::Import::CompanyRanking < Printsmith::Import::Base
-        offset: offset,
-        size: size,
+        offset:,
+        size:,

Apply the fix to this code:
@@ -40,10 +40,10 @@ class Printsmith::Import::CompanyRanking < Printsmith::Import::Base
 
-          company: company,
+          company:,
-          rank: rank,
-          type: type
+          rank:,
+          type:

File: app/services/printsmith/import/interactors/account/upsert.rb
Apply the fix to this code:
@@ -15,10 +15,10 @@ class Printsmith::Import::Interactors::Account::Upsert
-        account: account,
-        created: created,
-        modified: modified,
-        source: source
+        account:,
+        created:,
+        modified:,
+        source:

File: app/services/printsmith/import/interactors/adjustment/upsert.rb
Apply the fix to this code:
@@ -14,10 +14,10 @@ class Printsmith::Import::Interactors::Adjustment::Upsert
-        adjustment: adjustment,
-        created: created,
-        modified: modified,
-        source: source
+        adjustment:,
+        created:,
+        modified:,
+        source:

File: app/services/printsmith/import/interactors/contact/upsert.rb
Apply the fix to this code:
@@ -15,10 +15,10 @@ class Printsmith::Import::Interactors::Contact::Upsert
-        contact: contact,
-        created: created,
-        modified: modified,
-        source: source
+        contact:,
+        created:,
+        modified:,
+        source:

File: app/services/printsmith/import/interactors/inquiry/assign.rb
Apply the fix to this code:
@@ -13,8 +13,8 @@ class Printsmith::Import::Interactors::Inquiry::Assign
-        tenant: tenant,
-        estimate: estimate
+        tenant:,
+        estimate:

File: app/services/printsmith/import/interactors/invoice/assign.rb
Apply the fix to this code:
@@ -14,8 +14,8 @@ class Printsmith::Import::Interactors::Invoice::Assign
-        tenant: tenant,
-        invoice: invoice
+        tenant:,
+        invoice:

Apply the fix to this code:
@@ -49,7 +49,7 @@ class Printsmith::Import::Interactors::Invoice::Assign
-      estimate: estimate,
+      estimate:,

File: app/services/printsmith/import/interactors/invoice/create.rb
Apply the fix to this code:
@@ -14,10 +14,10 @@ class Printsmith::Import::Interactors::Invoice::Create
-        invoice: invoice,
-        created: created,
-        modified: modified,
-        source: source
+        invoice:,
+        created:,
+        modified:,
+        source:

File: app/services/printsmith/import/interactors/sales_rep/upsert.rb
Apply the fix to this code:
@@ -14,10 +14,10 @@ class Printsmith::Import::Interactors::SalesRep::Upsert
-        sales_rep: sales_rep,
-        created: created,
-        modified: modified,
-        source: source
+        sales_rep:,
+        created:,
+        modified:,
+        source:

File: app/services/printsmith_deprecated/export/archive_estimate.rb
Apply the fix to this code:
@@ -23,7 +23,7 @@ class PrintsmithDeprecated::Export::ArchiveEstimate < PrintsmithDeprecated::Expo
-        body: body,
+        body:,

File: app/services/printsmith_deprecated/export/base.rb
Apply the fix to this code:
@@ -25,7 +25,7 @@ class PrintsmithDeprecated::Export::Base
-          headers: parse_headers(headers: headers)
+          headers: parse_headers(headers:)

Apply the fix to this code:
@@ -37,7 +37,7 @@ class PrintsmithDeprecated::Export::Base
-          method: method,
+          method:,


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
