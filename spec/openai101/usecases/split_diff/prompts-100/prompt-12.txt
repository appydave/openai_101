In this process, we are reversing Ruby's shorthand hash syntax back to its explicit form.
Below are the specific diffs for each file. Apply the necessary fixes as outlined.
In this process, we are reversing Rubys shorthand hash syntax (e.g., `activity:`) back to its explicit form (e.g., `activity: activity`).

For example:
Before (shorthand): `activity:`
After (explicit): `activity: activity`

Similarly, if there are multiple key-value pairs:

Before (shorthand): `first_name:, last_name:, date_of_birth:`
After (explicit): `first_name: first_name, last_name: last_name, date_of_birth: date_of_birth`

Below are the specific diffs for each file. Apply the necessary fixes as outlined.


File: spec/services/printsmith/import/interactors/invoice/create_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, :api, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, :api, enterprise:) }

Apply the fix to this code:
@@ -16,7 +16,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Create, type: :services
 
-        let(:invoice_destination) { FactoryBot.build(:invoice, :trait1, tenant: tenant, sale_id: nil) }
+        let(:invoice_destination) { FactoryBot.build(:invoice, :trait1, tenant:, sale_id: nil) }

Apply the fix to this code:
@@ -33,7 +33,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Create, type: :services
 
-            hash_including(invoice: invoice_destination, tenant: tenant)
+            hash_including(invoice: invoice_destination, tenant:)

Apply the fix to this code:
@@ -50,7 +50,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Create, type: :services
 
-        let(:invoice_destination) { FactoryBot.build(:invoice, :trait1, tenant: tenant, sale_id: nil) }
+        let(:invoice_destination) { FactoryBot.build(:invoice, :trait1, tenant:, sale_id: nil) }

Apply the fix to this code:
@@ -80,7 +80,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Create, type: :services
 
-            hash_including(invoice: invoice_destination, tenant: tenant)
+            hash_including(invoice: invoice_destination, tenant:)

Apply the fix to this code:
@@ -97,7 +97,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Create, type: :services
 
-      let(:invoice_destination) { FactoryBot.build(:invoice, :trait1, tenant: tenant, sale_id: nil) }
+      let(:invoice_destination) { FactoryBot.build(:invoice, :trait1, tenant:, sale_id: nil) }

File: spec/services/printsmith/import/interactors/invoice/update_spec.rb
Apply the fix to this code:
@@ -4,12 +4,12 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, :api, enterprise: enterprise) }
-  let(:company) { FactoryBot.create(:company, :trait1, tenant: tenant) }
-  let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, :api, enterprise:) }
+  let(:company) { FactoryBot.create(:company, :trait1, tenant:) }
+  let(:contact) { FactoryBot.create(:contact, :trait1, tenant:) }
-  let!(:invoice) { FactoryBot.create(:invoice, :trait1, tenant: tenant, sale_id: nil) }
+  let!(:invoice) { FactoryBot.create(:invoice, :trait1, tenant:, sale_id: nil) }

Apply the fix to this code:
@@ -19,8 +19,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
 
-          invoice: invoice,
-          tenant: tenant,
+          invoice:,
+          tenant:,

Apply the fix to this code:
@@ -31,8 +31,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-              invoice: invoice,
-              tenant: tenant,
+              invoice:,
+              tenant:,

Apply the fix to this code:
@@ -48,8 +48,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-                invoice: invoice,
-                tenant: tenant,
+                invoice:,
+                tenant:,

Apply the fix to this code:
@@ -62,8 +62,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-                invoice: invoice,
-                tenant: tenant,
+                invoice:,
+                tenant:,

Apply the fix to this code:
@@ -79,8 +79,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-                invoice: invoice,
-                tenant: tenant,
+                invoice:,
+                tenant:,

Apply the fix to this code:
@@ -93,8 +93,8 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-                invoice: invoice,
-                tenant: tenant,
+                invoice:,
+                tenant:,

Apply the fix to this code:
@@ -112,7 +112,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
 
-            subject.execute(invoice: invoice, tenant: tenant)
+            subject.execute(invoice:, tenant:)

Apply the fix to this code:
@@ -126,7 +126,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
 
-            subject.execute(invoice: invoice, tenant: tenant)
+            subject.execute(invoice:, tenant:)

Apply the fix to this code:
@@ -135,7 +135,7 @@ RSpec.describe Printsmith::Import::Interactors::Invoice::Update, type: :services
-      let(:invoice) { FactoryBot.build(:invoice, :trait1, tenant: tenant) }
+      let(:invoice) { FactoryBot.build(:invoice, :trait1, tenant:) }

File: spec/services/printsmith/import/interactors/production_location/upsert_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:production_location) { FactoryBot.build(:production_location, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:production_location) { FactoryBot.build(:production_location, :trait2, tenant:) }

Apply the fix to this code:
@@ -16,10 +16,10 @@ RSpec.describe Printsmith::Import::Interactors::ProductionLocation::Upsert, type
-          production_location: production_location,
-          created: created,
-          modified: modified,
-          source: source
+          production_location:,
+          created:,
+          modified:,
+          source:

File: spec/services/printsmith/import/interactors/sales_base_tax/upsert_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:sales_base_tax) { FactoryBot.build(:sales_base_tax, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:sales_base_tax) { FactoryBot.build(:sales_base_tax, :trait2, tenant:) }

Apply the fix to this code:
@@ -18,10 +18,10 @@ RSpec.describe Printsmith::Import::Interactors::SalesBaseTax::Upsert, type: :ser
-            sales_base_tax: sales_base_tax,
-            created: created,
-            modified: modified,
-            source: source,
+            sales_base_tax:,
+            created:,
+            modified:,
+            source:,

Apply the fix to this code:
@@ -33,14 +33,14 @@ RSpec.describe Printsmith::Import::Interactors::SalesBaseTax::Upsert, type: :ser
 
-        let(:sales_base_tax_existing) { FactoryBot.create(:sales_base_tax, :trait2, tenant: tenant) }
+        let(:sales_base_tax_existing) { FactoryBot.create(:sales_base_tax, :trait2, tenant:) }
 
-            created: created,
-            modified: modified,
-            source: source,
+            created:,
+            modified:,
+            source:,

File: spec/services/printsmith/import/interactors/sales_rep/upsert_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:sales_rep) { FactoryBot.build(:sales_rep, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:sales_rep) { FactoryBot.build(:sales_rep, :trait2, tenant:) }

Apply the fix to this code:
@@ -16,10 +16,10 @@ RSpec.describe Printsmith::Import::Interactors::SalesRep::Upsert, type: :service
-          sales_rep: sales_rep,
-          created: created,
-          modified: modified,
-          source: source
+          sales_rep:,
+          created:,
+          modified:,
+          source:

File: spec/services/printsmith/import/interactors/sales_summary/upsert_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:sales_summary) { FactoryBot.build(:sales_summary, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:sales_summary) { FactoryBot.build(:sales_summary, :trait2, tenant:) }

Apply the fix to this code:
@@ -18,10 +18,10 @@ RSpec.describe Printsmith::Import::Interactors::SalesSummary::Upsert, type: :ser
-            sales_summary: sales_summary,
-            created: created,
-            modified: modified,
-            source: source,
+            sales_summary:,
+            created:,
+            modified:,
+            source:,

Apply the fix to this code:
@@ -34,14 +34,14 @@ RSpec.describe Printsmith::Import::Interactors::SalesSummary::Upsert, type: :ser
 
-        let(:sales_summary_existing) { FactoryBot.create(:sales_summary, :trait2, tenant: tenant, daily: true, monthly: false) }
+        let(:sales_summary_existing) { FactoryBot.create(:sales_summary, :trait2, tenant:, daily: true, monthly: false) }
 
-            created: created,
-            modified: modified,
-            source: source,
+            created:,
+            modified:,
+            source:,

File: spec/services/printsmith/import/interactors/sales_summary_pickup/upsert_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:sales_summary_pickup) { FactoryBot.build(:sales_summary_pickup, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:sales_summary_pickup) { FactoryBot.build(:sales_summary_pickup, :trait2, tenant:) }

Apply the fix to this code:
@@ -18,10 +18,10 @@ RSpec.describe Printsmith::Import::Interactors::SalesSummaryPickup::Upsert, type
-            sales_summary_pickup: sales_summary_pickup,
-            created: created,
-            modified: modified,
-            source: source,
+            sales_summary_pickup:,
+            created:,
+            modified:,
+            source:,

File: spec/services/printsmith/import/interactors/taken_by/create_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:estimate) { FactoryBot.create(:estimate, :trait2, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:estimate) { FactoryBot.create(:estimate, :trait2, tenant:) }

Apply the fix to this code:
@@ -13,7 +13,7 @@ RSpec.describe Printsmith::Import::Interactors::TakenBy::Create, type: :services
-          tenant: tenant,
+          tenant:,

File: spec/services/printsmith/import/interactors/tenant/update_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

Apply the fix to this code:
@@ -29,7 +29,7 @@ RSpec.describe Printsmith::Import::Interactors::Tenant::Update, type: :services
-              tenant: tenant,
+              tenant:,

Apply the fix to this code:
@@ -49,7 +49,7 @@ RSpec.describe Printsmith::Import::Interactors::Tenant::Update, type: :services
-              tenant: tenant,
+              tenant:,

Apply the fix to this code:
@@ -76,7 +76,7 @@ RSpec.describe Printsmith::Import::Interactors::Tenant::Update, type: :services
 
-              tenant: tenant,
+              tenant:,

Apply the fix to this code:
@@ -96,7 +96,7 @@ RSpec.describe Printsmith::Import::Interactors::Tenant::Update, type: :services
 
-              tenant: tenant,
+              tenant:,

Apply the fix to this code:
@@ -118,7 +118,7 @@ RSpec.describe Printsmith::Import::Interactors::Tenant::Update, type: :services
 
-              tenant: tenant,
+              tenant:,

Apply the fix to this code:
@@ -137,7 +137,7 @@ RSpec.describe Printsmith::Import::Interactors::Tenant::Update, type: :services
 
-                tenant: tenant,
+                tenant:,

Apply the fix to this code:
@@ -155,7 +155,7 @@ RSpec.describe Printsmith::Import::Interactors::Tenant::Update, type: :services
 
-                tenant: tenant,
+                tenant:,

File: spec/services/printsmith/import/invoice_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

Apply the fix to this code:
@@ -74,7 +74,7 @@ RSpec.describe Printsmith::Import::Invoice, type: :services do
 
-        let!(:invoice) { FactoryBot.create(:invoice, :trait1, platform_id: context_source["id"], tenant: tenant) }
+        let!(:invoice) { FactoryBot.create(:invoice, :trait1, platform_id: context_source["id"], tenant:) }

Apply the fix to this code:
@@ -108,9 +108,9 @@ RSpec.describe Printsmith::Import::Invoice, type: :services do
 
 
-      let(:invoice) { FactoryBot.create(:invoice, :trait2, tenant: tenant, platform_id: context_source["id"], sale_id: nil) }
+      let(:invoice) { FactoryBot.create(:invoice, :trait2, tenant:, platform_id: context_source["id"], sale_id: nil) }
-        invoice: invoice,
+        invoice:,

File: spec/services/printsmith/import/job_titles_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

File: spec/services/printsmith/import/lead_source_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

File: spec/services/printsmith/import/monthly_sales_base_tax_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

File: spec/services/printsmith/import/monthly_sales_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

File: spec/services/printsmith/import/monthly_sales_summary_pickups_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

File: spec/services/printsmith/import/production_location_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

File: spec/services/printsmith/import/sales_rep_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

File: spec/services/printsmith_deprecated/export/archive_estimate_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant:) }

Apply the fix to this code:
@@ -76,7 +76,7 @@ RSpec.describe PrintsmithDeprecated::Export::ArchiveEstimate, type: :services do
 
-        let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant) }
+        let(:contact) { FactoryBot.create(:contact, :trait1, tenant:) }

Apply the fix to this code:
@@ -100,7 +100,7 @@ RSpec.describe PrintsmithDeprecated::Export::ArchiveEstimate, type: :services do
 
-            ).to receive(:request).with(/Estimate/ , hash_including({body: body})).and_return(api_response)
+            ).to receive(:request).with(/Estimate/ , hash_including({body:})).and_return(api_response)

File: spec/services/printsmith_deprecated/export/base_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }

Apply the fix to this code:
@@ -37,7 +37,7 @@ RSpec.describe PrintsmithDeprecated::Export::Base, type: :services do
-      let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
+      let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }

File: spec/services/printsmith_deprecated/export/company_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:company) { FactoryBot.create(:company, :trait1, tenant: tenant, platform_id: 12345) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:company) { FactoryBot.create(:company, :trait1, tenant:, platform_id: 12345) }

File: spec/services/printsmith_deprecated/export/contact_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant, platform_id: 12345, temp: false, deleted: false) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:contact) { FactoryBot.create(:contact, :trait1, tenant:, platform_id: 12345, temp: false, deleted: false) }

File: spec/services/printsmith_deprecated/export/estimate_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant: tenant) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:estimate) { FactoryBot.create(:estimate, :trait1, tenant:) }

Apply the fix to this code:
@@ -60,8 +60,8 @@ RSpec.describe PrintsmithDeprecated::Export::Estimate, type: :services do
-      let(:sales_rep) { FactoryBot.create(:sales_rep, :trait1, tenant: tenant, platform_id: "test") }
-      let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant) }
+      let(:sales_rep) { FactoryBot.create(:sales_rep, :trait1, tenant:, platform_id: "test") }
+      let(:contact) { FactoryBot.create(:contact, :trait1, tenant:) }

Apply the fix to this code:
@@ -95,14 +95,14 @@ RSpec.describe PrintsmithDeprecated::Export::Estimate, type: :services do
-              wanted_by: wanted_by
+              wanted_by:
 
-            ).to receive(:request).with(/Estimate/ , hash_including({body: body})).and_return(api_response)
+            ).to receive(:request).with(/Estimate/ , hash_including({body:})).and_return(api_response)

Apply the fix to this code:
@@ -112,7 +112,7 @@ RSpec.describe PrintsmithDeprecated::Export::Estimate, type: :services do
-              wanted_by: wanted_by
+              wanted_by:

Apply the fix to this code:
@@ -122,7 +122,7 @@ RSpec.describe PrintsmithDeprecated::Export::Estimate, type: :services do
-            ).to receive(:request).with(/Estimate/ , hash_including({body: body})).and_return(api_response)
+            ).to receive(:request).with(/Estimate/ , hash_including({body:})).and_return(api_response)

File: spec/services/printsmith_deprecated/export/invoice_spec.rb
Apply the fix to this code:
@@ -4,8 +4,8 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise: enterprise) }
-  let(:invoice) { FactoryBot.create(:invoice, :trait1, tenant: tenant, sale_id: nil) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait2, enterprise:) }
+  let(:invoice) { FactoryBot.create(:invoice, :trait1, tenant:, sale_id: nil) }

Apply the fix to this code:
@@ -60,8 +60,8 @@ RSpec.describe PrintsmithDeprecated::Export::Invoice, type: :services do
-      let(:sales_rep) { FactoryBot.create(:sales_rep, :trait1, tenant: tenant, platform_id: "test") }
-      let(:contact) { FactoryBot.create(:contact, :trait1, tenant: tenant) }
+      let(:sales_rep) { FactoryBot.create(:sales_rep, :trait1, tenant:, platform_id: "test") }
+      let(:contact) { FactoryBot.create(:contact, :trait1, tenant:) }

Apply the fix to this code:
@@ -99,7 +99,7 @@ RSpec.describe PrintsmithDeprecated::Export::Invoice, type: :services do
-              proof_by: proof_by,
+              proof_by:,

Apply the fix to this code:
@@ -121,7 +121,7 @@ RSpec.describe PrintsmithDeprecated::Export::Invoice, type: :services do
-              proof_by: proof_by,
+              proof_by:,

Apply the fix to this code:
@@ -134,7 +134,7 @@ RSpec.describe PrintsmithDeprecated::Export::Invoice, type: :services do
-            ).to receive(:request).with(/Invoice/ , hash_including({body: body})).and_return(api_response)
+            ).to receive(:request).with(/Invoice/ , hash_including({body:})).and_return(api_response)

Apply the fix to this code:
@@ -149,7 +149,7 @@ RSpec.describe PrintsmithDeprecated::Export::Invoice, type: :services do
 
-          ).to receive(:request).with(/Invoice/ , hash_including({body: body})).and_return(api_response)
+          ).to receive(:request).with(/Invoice/ , hash_including({body:})).and_return(api_response)

File: spec/services/printsmith_deprecated/import/association_resolver_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:) }

Apply the fix to this code:
@@ -14,7 +14,7 @@ RSpec.describe PrintsmithDeprecated::Import::AssociationResolver do
-          tenant: tenant,
+          tenant:,

Apply the fix to this code:
@@ -22,7 +22,7 @@ RSpec.describe PrintsmithDeprecated::Import::AssociationResolver do
 
-        let!(:production_location) { FactoryBot.create(:production_location, :trait1, tenant: tenant, platform_id: estimate.documentlocation_id) }
+        let!(:production_location) { FactoryBot.create(:production_location, :trait1, tenant:, platform_id: estimate.documentlocation_id) }

Apply the fix to this code:
@@ -54,7 +54,7 @@ RSpec.describe PrintsmithDeprecated::Import::AssociationResolver do
-          tenant: tenant,
+          tenant:,

File: spec/services/reports/qvinci/api/balance_sheet_spec.rb
Apply the fix to this code:
@@ -5,7 +5,7 @@ require "rails_helper"
-    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, qvinci_location_ids: 12345) }
+    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, qvinci_location_ids: 12345) }

Apply the fix to this code:
@@ -38,9 +38,9 @@ RSpec.describe Reports::Qvinci::Api::BalanceSheet, type: :services do
-          tenant: tenant,
-          start_date: start_date,
-          end_date: end_date
+          tenant:,
+          start_date:,
+          end_date:

File: spec/services/reports/qvinci/api/base_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, qvinci_location_ids: 12345) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, qvinci_location_ids: 12345) }

Apply the fix to this code:
@@ -19,9 +19,9 @@ RSpec.describe Reports::Qvinci::Api::Base, type: :services do
-        tenant: tenant,
-        start_date: start_date,
-        end_date: end_date
+        tenant:,
+        start_date:,
+        end_date:

Apply the fix to this code:
@@ -78,17 +78,19 @@ RSpec.describe Reports::Qvinci::Api::Base, type: :services do
-              body: {}.to_json,
-              headers: {
-                "Content-Type" => "application/json",
-                "X-apiToken" => "12345"
-              }
+              hash_including(
+                body: {}.to_json,
+                headers: {
+                  "Content-Type" => "application/json",
+                  "X-apiToken" => "12345"
+                }
+              )
 
-            api.request("BalanceSheet", params: params)
+            api.request("BalanceSheet", params:)

File: spec/services/reports/qvinci/api/profit_and_loss_spec.rb
Apply the fix to this code:
@@ -5,7 +5,7 @@ require "rails_helper"
-    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, qvinci_location_ids: 12345) }
+    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, qvinci_location_ids: 12345) }

Apply the fix to this code:
@@ -50,9 +50,9 @@ RSpec.describe Reports::Qvinci::Api::ProfitAndLoss, type: :services do
-          tenant: tenant,
-          start_date: start_date,
-          end_date: end_date
+          tenant:,
+          start_date:,
+          end_date:

Apply the fix to this code:
@@ -75,9 +75,9 @@ RSpec.describe Reports::Qvinci::Api::ProfitAndLoss, type: :services do
-          tenant: tenant,
-          start_date: start_date,
-          end_date: end_date
+          tenant:,
+          start_date:,
+          end_date:

File: spec/services/reports/qvinci/business_plan/aggregator_spec.rb
Apply the fix to this code:
@@ -9,7 +9,7 @@ RSpec.describe Reports::Qvinci::BusinessPlan::Aggregator, type: :services do
-    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, qvinci_location_ids: 12345) }
+    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, qvinci_location_ids: 12345) }

Apply the fix to this code:
@@ -57,10 +57,10 @@ RSpec.describe Reports::Qvinci::BusinessPlan::Aggregator, type: :services do
 
-        subject.execute({
-          tenant: tenant,
-          run_date: run_date # no_of_years default is 4
-        })
+        subject.execute(
+          tenant:,
+          run_date: # no_of_years default is 4
+        )

Apply the fix to this code:
@@ -71,8 +71,8 @@ RSpec.describe Reports::Qvinci::BusinessPlan::Aggregator, type: :services do
-          tenant: tenant,
-          run_date: run_date,
+          tenant:,
+          run_date:,

Apply the fix to this code:
@@ -85,8 +85,8 @@ RSpec.describe Reports::Qvinci::BusinessPlan::Aggregator, type: :services do
-          tenant: tenant,
-          run_date: run_date,
+          tenant:,
+          run_date:,

Apply the fix to this code:
@@ -99,7 +99,7 @@ RSpec.describe Reports::Qvinci::BusinessPlan::Aggregator, type: :services do
-          tenant: tenant,
+          tenant:,

Apply the fix to this code:
@@ -113,7 +113,7 @@ RSpec.describe Reports::Qvinci::BusinessPlan::Aggregator, type: :services do
-          tenant: tenant,
+          tenant:,

File: spec/services/reports/qvinci/business_plan/kpi_spec.rb
Apply the fix to this code:
@@ -5,7 +5,7 @@ require "rails_helper"
-    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, qvinci_location_ids: 12345) }
+    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, qvinci_location_ids: 12345) }

File: spec/services/reports/qvinci/import/balance_sheet_spec.rb
Apply the fix to this code:
@@ -5,7 +5,7 @@ require "rails_helper"
-    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, qvinci_location_ids: 12345) }
+    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, qvinci_location_ids: 12345) }

Apply the fix to this code:
@@ -24,23 +24,23 @@ RSpec.describe Reports::Qvinci::Import::BalanceSheet, type: :services do
-                              tenant: tenant,
-                              start_date: start_date,
-                              end_date: end_date
+                              tenant:,
+                              start_date:,
+                              end_date:
 
-        let!(:json_store) { FactoryBot.create(:json_store, enterprise_id: enterprise.id, tenant_id: tenant.id, key: key, data: nil) }
+        let!(:json_store) { FactoryBot.create(:json_store, enterprise_id: enterprise.id, tenant_id: tenant.id, key:, data: nil) }
 
-                            tenant: tenant,
-                            start_date: start_date,
-                            end_date: end_date
+                            tenant:,
+                            start_date:,
+                            end_date:

Apply the fix to this code:
@@ -54,23 +54,23 @@ RSpec.describe Reports::Qvinci::Import::BalanceSheet, type: :services do
-                              tenant: tenant,
-                              start_date: start_date,
-                              end_date: end_date
+                              tenant:,
+                              start_date:,
+                              end_date:
 
-        let!(:json_store) { FactoryBot.create(:json_store, enterprise_id: enterprise.id, tenant_id: tenant.id, key: key, data: nil) }
+        let!(:json_store) { FactoryBot.create(:json_store, enterprise_id: enterprise.id, tenant_id: tenant.id, key:, data: nil) }
 
-                            tenant: tenant,
-                            start_date: start_date,
-                            end_date: end_date
+                            tenant:,
+                            start_date:,
+                            end_date:

File: spec/services/reports/qvinci/import/profit_and_loss_spec.rb
Apply the fix to this code:
@@ -5,7 +5,7 @@ require "rails_helper"
-    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, qvinci_location_ids: 12345) }
+    let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, qvinci_location_ids: 12345) }

Apply the fix to this code:
@@ -29,9 +29,9 @@ RSpec.describe Reports::Qvinci::Import::ProfitAndLoss, type: :services do
-                              tenant: tenant,
-                              start_date: start_date,
-                              end_date: end_date,
+                              tenant:,
+                              start_date:,
+                              end_date:,

Apply the fix to this code:
@@ -41,14 +41,14 @@ RSpec.describe Reports::Qvinci::Import::ProfitAndLoss, type: :services do
 
-        let!(:json_store) { FactoryBot.create(:json_store, enterprise_id: enterprise.id, tenant_id: tenant.id, key: key, data: nil) }
+        let!(:json_store) { FactoryBot.create(:json_store, enterprise_id: enterprise.id, tenant_id: tenant.id, key:, data: nil) }
 
-                            tenant: tenant,
-                            start_date: start_date,
-                            end_date: end_date,
+                            tenant:,
+                            start_date:,
+                            end_date:,

Apply the fix to this code:
@@ -65,23 +65,23 @@ RSpec.describe Reports::Qvinci::Import::ProfitAndLoss, type: :services do
-                              tenant: tenant,
-                              start_date: start_date,
-                              end_date: end_date
+                              tenant:,
+                              start_date:,
+                              end_date:
 
-        let!(:json_store) { FactoryBot.create(:json_store, enterprise_id: enterprise.id, tenant_id: tenant.id, key: key, data: nil) }
+        let!(:json_store) { FactoryBot.create(:json_store, enterprise_id: enterprise.id, tenant_id: tenant.id, key:, data: nil) }
 
-                            tenant: tenant,
-                            start_date: start_date,
-                            end_date: end_date
+                            tenant:,
+                            start_date:,
+                            end_date:

Apply the fix to this code:
@@ -95,9 +95,9 @@ RSpec.describe Reports::Qvinci::Import::ProfitAndLoss, type: :services do
-                              tenant: tenant,
-                              start_date: start_date,
-                              end_date: end_date,
+                              tenant:,
+                              start_date:,
+                              end_date:,

File: spec/services/reports/qvinci/import/refresh_kpi_spec.rb
Apply the fix to this code:
@@ -4,7 +4,7 @@ require "rails_helper"
 
-  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise: enterprise, qvinci_location_ids: 12345) }
+  let(:tenant) { FactoryBot.create(:tenant, :trait1, enterprise:, qvinci_location_ids: 12345) }

Apply the fix to this code:
@@ -37,7 +37,7 @@ RSpec.describe Reports::Qvinci::Import::RefreshKpi, type: :service do
 
 
-      described_class.new(tenant: tenant).send(:fetch_reports)
+      described_class.new(tenant:).send(:fetch_reports)

Apply the fix to this code:
@@ -46,7 +46,7 @@ RSpec.describe Reports::Qvinci::Import::RefreshKpi, type: :service do
 
-      described_class.new(tenant: tenant).send(:fetch_reports)
+      described_class.new(tenant:).send(:fetch_reports)

Apply the fix to this code:
@@ -59,13 +59,13 @@ RSpec.describe Reports::Qvinci::Import::RefreshKpi, type: :service do
 
-      described_class.new(tenant: tenant).send(:fetch_reports)
+      described_class.new(tenant:).send(:fetch_reports)
 
 
-      expect(described_class.new(tenant: tenant).send(:fetch_reports)).to be_nil
+      expect(described_class.new(tenant:).send(:fetch_reports)).to be_nil

File: spec/support/shared_examples/pagination.rb
Apply the fix to this code:
@@ -16,7 +16,7 @@ RSpec.shared_examples :pagination do
 
-      let(:params) { { page: page_number, page_size: page_size } }
+      let(:params) { { page: page_number, page_size: } }


Once you've completed the changes, ensure all modified code follows the style guide and is tested.
